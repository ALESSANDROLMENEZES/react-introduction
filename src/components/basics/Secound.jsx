import React from 'react';

export default props => (
  /* jshint ignore:start */
  <div>
    <h2>{props.title}</h2>
    <h3>{props.subtitle}</h3>
    <p>{props.description}</p>
  </div>
  /* jshint ignore:end */
);